# Runtime dependencies for the benchmark harness + cost model.
# (pyproject.toml is the source of truth; this file mirrors the runtime deps
#  for a quick `pip install -r requirements.txt`.)
#
# Dev tooling:        pip install -e ".[dev]"      (ruff, pytest)
# Notebook/analysis:  pip install -e ".[analysis]" (numpy, pandas, matplotlib, jupyter)

httpx>=0.27
PyYAML>=6.0
